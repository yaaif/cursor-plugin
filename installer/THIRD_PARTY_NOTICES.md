# Third-party notices

Native installer packages may include an official **Node.js** runtime from
https://nodejs.org/dist/ (currently v22.22.2 LTS “Jod”).

Node.js is copyright OpenJS Foundation and other contributors. It is
distributed under the MIT License. See https://github.com/nodejs/node/blob/main/LICENSE.

Archives are verified against `SHASUMS256.txt` from the same release directory
before they are unpacked. Checksums are also pinned in
`installer/runtime-manifest.json`.

The YAA\\F Cursor plugin itself is Apache-2.0 (see `LICENSE` in the repository
root).
