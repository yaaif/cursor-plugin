---
name: yaaif-new-mcp
description: Scaffold, deploy, and register MCP tools on YAA\F
---

Use the `yaaif-create-mcp` skill. Ensure auth first. Contract → scaffold → image → `yaaif_mcp_deployment_*` → verify catalog (or `yaaif_mcp_link_or_create` fallback).
